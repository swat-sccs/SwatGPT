[project]
name = "ingest"
version = "0.1.0"
description = "SwatGPT knowledge-base ingestion: markdown -> chunks -> TEI embeddings -> Qdrant"
requires-python = ">=3.11"
dependencies = [
    "llama-index-core>=0.11,<0.14",
    "python-frontmatter>=1.1",
    "qdrant-client>=1.12",
    "httpx>=0.27",
]
