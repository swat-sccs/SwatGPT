/**
 * Owned by the oversight work (Layer C). Pure keyword/regex screen applied to
 * the user prompt and assistant reply when a generation is recorded; each
 * returned string becomes one Flag `reason`.
 */
export function findKeywordFlags(_text: string): string[] {
  return [];
}
